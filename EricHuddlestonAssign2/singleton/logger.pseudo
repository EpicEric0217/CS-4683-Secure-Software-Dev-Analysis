public class Logger {
	private String log;
	// Constructor
	public Logger(){
	}

	// Log something
	public info(String information){
		this.log.append(information);
	}
}
/** CHANGE:  Here I add in my own constructor, a no argument constructor. **/
public class Logger() {
    log = new Logger();
// Here I allow instantiation once.
    logger.log(string information);
    String contents = logger.getContents();

}